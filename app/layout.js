export const metadata = {
    title: 'PSF Inventario',
    description: 'Dashboard de Productos Terminados',
};

export default function RootLayout({ children }) {
    return (
          <html lang="es">
            <head />
            <body>{children}</body>
      </html>
    );
}
